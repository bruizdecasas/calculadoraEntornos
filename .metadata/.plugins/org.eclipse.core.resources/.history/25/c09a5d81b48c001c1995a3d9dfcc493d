package ed_calculadora;

/**
 * Esta clase implementará todos los métodos que están relacionados con la resta de la calculadora.
 * <br>
 * Esta clase tendrá los siguientes atributos:
 * <br>
 * <b>Resta dos números reales.</b>
 * <br>
 * <b>Resta dos números enteros.</b>
 * <br>
 * <b>Resta tres números reales.</b>
 * <br>
 * <b>Resta acumulada</b>
 * <br>
 * <br>
 * <b> Casos especiales: </b>
 * <br>
 * - En el caso de que <b>no se admitieran números negativos</b> habría que 
 *  especificarlo al usuario y ademas utilizar un if para en el caso de que lo pusiera le
 *  indicara el método que debe ser un número mayor o igual que cero.
 * <br>
 * - Cuando un <b>parámetro es 0</b> no ocurre nada especial.
 * <br>
 * <ul>
 * 
 * 
 * @authors Diego García Rodriguez
 * @version 2.0
 *
 */

  public class Restar {
	
		
	 /** 
	  * Se crea el método para la resta de dos números reales.
	  * 
	  * @param num número real que es el minuendo.
	  * @param num1 número real que es el sustraendo.
	  * @return Devuelve el resultado de la resta.
	  *
	  */
	
	    public double Resta (double num, double num1) {
		  //TODO
		  return Resta;
		}
	  
	/**
	 * Se crea un método para la resta de números enteros.
	 * 
	 * @param num11 número entero que es el minuendo.
	 * @param  num12 número entero que es el sustraendo.
	 * @return Devuelve el resultado de la resta. 
	 * 
	 */
		
	    public int Resta (int num11, int num12) {
			//TODO
			return Resta;
		}
		
	/**
	 * Se crea un método para la resta de tres números reales.
	 * 
	 * @param num21 número real que es el minuendo.
	 * @param num22 número real que es el sustraendo.
	 * @param num23 número real que es el sustraendo.
	 * @return Devuelve el el resultado de la resta.
	 */
		
		public double Resta (double num21, double num22, double num23) {
			//TODO
			return Resta;
		}
		
	/**
	 * Se crea un método Acumulador, que irá acumulado el resultado de cada resta que se realice.
	 * 
	 * @param num1 es un número real en que se van acumulando resultados.
	 * 
	 * El Contador se inicializa en 0
	 * 
	 */
		public void acumulador (double num1) {
			//TODO
		}
	}

